# EXAONE-4.5-33B-AWQ-R4 정성평가 요약

- 평가 대상: `/mnt/quant-data/work/awq/exaone45-33b-awq-4bit-fullstruct-mtp-r4-chat128-l512`
- 평가 샘플: `configs/evals/exaone45_awq_qual_samples_v1.json` (16개)
- 실행 스크립트: `scripts/benchmark/exaone45_awq_qual_eval.py`
- 서빙 환경: `uv` venv `/home/lsj/venvs/exaone_eval`
- 평가 방식: OpenAI-compatible `vllm serve` + deterministic completions (`temperature=0`)

## 샘플 구성
- wiki 6개: 정의/희소 고유명사/역사 요약/과학 설명/재서술
- qa 6개: 단문 질답/개발자 QA/기초 수 비교/bullet 요약/판단 근거
- textbook 4개: 설명형 장문/찬반 균형/교육 개념 비교/추상적 논의

## 자동 집계
- raw summary: pass 8 / mixed 4 / fail 4
- 주의: 이 집계는 키워드/반복률 기반의 기계적 분류라 다소 낙관적이다.

## 수동 정성평가
전체적으로 "짧은 구조화 응답의 앞부분은 그럭저럭 맞지만, 길이가 조금만 늘어나도 반복 루프나 템플릿 누수로 무너진다"는 경향이 강했다.

### 상대적으로 괜찮았던 항목
1. `wiki-05` 광합성 설명
   - 앞 3~4문장은 자연스럽고 의미 전달도 된다.
   - 다만 뒤에서 같은 설명을 다시 반복한다.
2. `wiki-06` 문장 다듬기
   - 첫 문장은 거의 바로 사용 가능하다.
   - 뒤에 불필요한 해설을 길게 덧붙이며 늘어진다.
3. `qa-03` 9.9 vs 9.11 비교
   - 핵심 판단 자체는 맞다.
   - 답 뒤에 불필요한 다음 문제 생성이 붙는다.
4. `qa-05` 3개 bullet 요약
   - 첫 bullet 3개는 요구에 맞는다.
   - 이후 동일 형식을 반복 재생산한다.
5. `textbook-04` 예술의 가치 vs 금전
   - 첫 3문장은 비교적 매끄럽다.
   - 끝부분에 숫자 목록 루프가 붙는다.

### 경계가 필요한 항목
1. `wiki-04` 태양광 장단점
   - 시작은 맞지만 중후반부터 특정 단어를 과도 반복한다.
2. `textbook-02` 창의성 찬반 정리
   - 주제는 맞지만 문장이 지나치게 장황하고 열거 반복으로 붕괴한다.
3. `qa-01` 오늘 날씨
   - 현재 날씨를 알 수 없다고 제한을 설명해야 하는데, 임의의 날씨를 단정해 말한다.
   - 추론 태그/기호 잡음도 섞여 있다.

### 명확히 불량했던 항목
- `wiki-01`, `wiki-02`, `wiki-03`: 희소 위키/역사성 프롬프트에서 엉뚱한 숫자 반복, 문자열 루프, 구조 붕괴가 나타났다.
- `qa-02`: 개발자 QA에서 프롬프트 자체를 반복 복사하는 형태로 실패했다.
- `qa-04`: 자기소개 프롬프트에서 `:` 문자 루프가 발생했다.
- `qa-06`: `</think>` 같은 내부 추론 태그가 노출되고 답변 형식이 무너졌다.
- `textbook-01`, `textbook-03`: 설명형 장문에서 문두 반복/프롬프트 에코가 심하다.

## 종합 판단
- 짧고 단순한 텍스트 작업은 "첫 몇 줄"만 보면 어느 정도 쓸 수 있다.
- 하지만 현재 R4 산출물은 조금만 길어져도 반복/에코/추론 태그 누수 문제가 자주 나온다.
- 특히 다음 유형에서 취약하다:
  - 희소 고유명사/역사 요약
  - 장문 설명형 응답
  - 개발자 QA처럼 형식 제약이 있는 질답
- 따라서 현 상태를 "배포 가능 품질"로 보기는 어렵고,
  - 반복 억제,
  - reasoning/template 누수 제거,
  - 장문 안정성
  을 추가로 확인해야 한다.

## 실무적 한 줄 결론
현재 R4는 "짧은 답변 초반부는 일부 usable, 하지만 전체 응답 안정성은 아직 부족"이다.
